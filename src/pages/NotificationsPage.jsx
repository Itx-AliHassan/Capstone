import {Bell,Check} from 'lucide-react';import {useWorkspace} from '../context/WorkspaceContext';export default function NotificationsPage(){const{notifications,markRead,markAllRead}=useWorkspace();return <div className="max-w-3xl space-y-5"><div className="flex justify-between items-end"><div><p className="text-sm text-slate-500">In-app</p><h1 className="text-2xl font-semibold">Notifications</h1></div><button className="secondary" onClick={markAllRead}><Check size={15}/> Mark all read</button></div><div className="card divide-y dark:divide-slate-800">{notifications.map(n=><button key={n.id} onClick={()=>markRead(n.id)} className={`w-full text-left p-4 flex gap-3 ${n.read?'opacity-50':''}`}><Bell size={17} className="mt-1"/><div><b className="text-sm">{n.text}</b><div className="text-xs text-slate-500 mt-1">{n.createdAt?new Date(n.createdAt).toLocaleString():'Just now'}</div></div></button>)}{!notifications.length&&<div className="empty">No notifications.</div>}</div></div>}
