import { useWorkspace } from '../context/WorkspaceContext'
export default function ActivityPage(){const {tasks}=useWorkspace();const events=tasks.map((t,i)=>({id:t.id,text:`${i%2?'Updated':'Created'} “${t.title}”`,user:i%2?'Maya Chen':'Demo User',time:`${i+1}h ago`}));return <div className="max-w-4xl"><h1 className="text-2xl font-semibold dark:text-white mb-5">Activity</h1><div className="card divide-y divide-slate-100 dark:divide-slate-800">{events.map(e=><div key={e.id} className="p-4 flex gap-3"><div className="w-2 h-2 rounded-full bg-blue-600 mt-2"/><div className="flex-1"><div className="text-sm dark:text-white">{e.text}</div><div className="text-xs text-slate-500 mt-1">{e.user}</div></div><span className="text-xs text-slate-400">{e.time}</span></div>)}</div></div>}
