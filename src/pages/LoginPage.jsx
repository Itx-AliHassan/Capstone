import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { useAuth } from '../context/AuthContext'
export default function LoginPage(){const {login,loginGoogle}=useAuth();const nav=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');async function submit(e){e.preventDefault();try{await login(email,password);nav('/')}catch(err){setError(err.message)}}return <AuthShell title="Welcome back" subtitle="Sign in to your workspace"><form onSubmit={submit} className="space-y-3"><input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="Email" className="field"/><input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="Password" className="field"/>{error&&<p className="text-red-500 text-sm">{error}</p>}<button className="primary w-full">Sign in</button></form><button onClick={async()=>{await loginGoogle();nav('/')}} className="secondary w-full mt-3">Continue with Google</button><p className="text-sm text-slate-500 mt-5">No account? <Link className="text-blue-600" to="/signup">Create one</Link></p></AuthShell>}
function AuthShell({title,subtitle,children}){return <div className="min-h-screen grid place-items-center bg-slate-50 dark:bg-[#090D16] p-4"><div className="w-full max-w-md rounded-3xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 p-7 shadow-sm"><div className="h-10 w-10 rounded-xl bg-blue-600 grid place-items-center text-white font-bold mb-5">W</div><h1 className="text-2xl font-semibold text-slate-900 dark:text-white">{title}</h1><p className="text-slate-500 mt-1 mb-6">{subtitle}</p>{children}</div></div>}
