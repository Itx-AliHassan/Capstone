import {useEffect,useState} from 'react';import {useNavigate} from 'react-router-dom';import {Search} from 'lucide-react';export default function CommandPalette(){const[open,setOpen]=useState(false);const[q,setQ]=useState('');const nav=useNavigate();useEffect(()=>{const h=()=>setOpen(true);const k=e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();setOpen(true)}};addEventListener('open-command',h);addEventListener('keydown',k);return()=>{removeEventListener('open-command',h);removeEventListener('keydown',k)}},[]);if(!open)return null;const cmds=[['Home','/'],['Activity','/activity'],['Profile','/profile'],['Workspace settings','/workspace/settings'],['Data & Sync','/data']].filter(x=>x[0].toLowerCase().includes(q.toLowerCase()));return <div className="fixed inset-0 z-50 bg-black/40 p-4" onClick={()=>setOpen(false)}><div className="max-w-xl mx-auto mt-20 card overflow-hidden" onClick={e=>e.stopPropagation()}><div className="flex items-center gap-2 p-3 border-b dark:border-slate-800"><Search size={17}/><input autoFocus className="bg-transparent outline-none flex-1" placeholder="Type a command…" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Escape'&&setOpen(false)}/></div>{cmds.map(([l,to])=><button key={to} onClick={()=>{nav(to);setOpen(false)}} className="w-full text-left px-4 py-3 hover:bg-slate-50 dark:hover:bg-slate-800">{l}</button>)}</div></div>}
