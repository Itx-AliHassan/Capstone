import {Bell,Search,Sun,Moon,Undo2,Redo2,Command,Menu} from 'lucide-react';import {useWorkspace} from '../context/WorkspaceContext';import {useTheme} from '../context/ThemeContext';import {useState} from 'react';import {useNavigate} from 'react-router-dom'
export default function Topbar(){const {notifications,markAllRead,undo,redo}=useWorkspace();const {theme,toggleTheme}=useTheme();const [q,setQ]=useState('');const nav=useNavigate();return <header className="h-16 border-b border-slate-200 dark:border-slate-800 bg-white/90 dark:bg-slate-950/90 backdrop-blur sticky top-0 z-20 flex items-center gap-2 px-4"><button className="md:hidden icon-btn"><Menu size={18}/></button><div className="relative flex-1 max-w-xl"><Search size={16} className="absolute left-3 top-3 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&nav(`/search?q=${encodeURIComponent(q)}`)} placeholder="Search everything… (Enter)" className="field pl-9"/></div><button className="icon-btn" title="Undo" onClick={undo}><Undo2 size={17}/></button><button className="icon-btn" title="Redo" onClick={redo}><Redo2 size={17}/></button><button className="icon-btn" title="Command palette" onClick={()=>window.dispatchEvent(new Event('open-command'))}><Command size={17}/></button><button className="icon-btn" onClick={toggleTheme}>{theme==='dark'?<Sun size={17}/>:<Moon size={17}/>}</button><button className="relative icon-btn" onClick={markAllRead}><Bell size={17}/>{notifications.filter(n=>!n.read).length>0&&<span className="absolute -top-1 -right-1 w-4 h-4 text-[9px] rounded-full bg-blue-600 text-white grid place-items-center">{notifications.filter(n=>!n.read).length}</span>}</button></header>}
